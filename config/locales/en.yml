# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  spree:
    themes: Themes
    templates: Templates
    path: Path
    format: Format
    handler: Handler
    body: Body
    name: Name
    state: Theme State
    theme_states:
      compiled: Compiled
      drafted: Drafted
      published: Published

    actions:
      upload_theme: Upload Themes
      upload: Upload
      compiled: Compile
      published: Publish
      view_templates: Templates
      new_template: New Template

    models:
      theme:
        minimum_active_error: Atleast one theme should be published.

    flash:
      admin:
        themes:
          upload:
            success: "Theme %{name} successfully uploaded."
            failure: "Something went wrong, theme could not be uploaded."
          state_change:
            success: "Theme %{name} successfully %{state}."
            failure: "Something went wrong, theme %{name} could not be %{state}."
